const state = () => {
  return {
    count: '90',
  };
};
const mutations = {};
const getters = {};
const actions = {};
const user = {
  namespaced: true,
  state,
  mutations,
  getters,
  actions,
};
export default user;
